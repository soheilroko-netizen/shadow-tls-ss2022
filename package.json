{
  "name": "dakal-tls",
  "version": "9.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp public/index.html dist/index.html && if exist public\"*.woff2 (copy public\"*.woff2 dist/) else (echo no fonts)"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
